# Core platform dependencies
fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0,<0.30.0
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
python-multipart>=0.0.6
httpx>=0.25.0,<0.28.0

# Search & indexing
whoosh>=2.7.4,<3.0.0

# Janus (Prolog integration) - Janus is typically an asyncio library; Prolog interop via pyswip or similar
# Note: Janus Prolog adapter may require SWI-Prolog and pyswip installation
# pyswip>=0.2.9  # optional, for Prolog integration

# Hy (Lisp dialect)
hy>=1.0.0a4,<2.0.0

# Testing
pytest>=7.4.0,<8.0.0
pytest-asyncio>=0.21.0,<0.27.0
pytest-cov>=4.1.0,<6.0.0

# Dev utilities
ruff>=0.1.0  # linter
python-dotenv>=1.0.0,<2.0.0

# Logging
structlog>=23.0.0,<25.0.0